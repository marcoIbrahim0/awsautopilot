# Live SaaS E2E Testing Docs (Consolidated)

This folder contains the full documentation set used for tracker-driven live SaaS E2E execution.

> ⚠️ Local evidence note: `docs/test-results/live-runs/` was deleted from this workspace on March 14, 2026 to reclaim disk space, then selectively rebuilt for retained March 15-18 proof packages. Some older historical run links may still be absent locally; use `.cursor/notes/task_log.md` and `.cursor/notes/task_index.md` for retained run summaries.

> ⚠️ Trust-review note: older live-run artifacts can still contain deprecated `SAAS_BUNDLE_RUNNER_TEMPLATE_*` settings or raw internal evidence dumps captured before the current trust package existed. For buyer/security review, use [docs/trust/README.md](/Users/marcomaher/AWS%20Security%20Autopilot/docs/trust/README.md) and the sanitized package under [docs/test-results/trust-package/](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/trust-package/20260320T023532Z-buyer-trust-package/README.md).

> Live UI validation rule: for browser-visible fixes, retain a console dump alongside screenshots and treat React production errors, hydration mismatches, or other console-visible runtime errors as failures unless the run summary explicitly waives them.

## Files in this Folder

- `00-BASE-ISSUE-TRACKER.md` — Primary issue tracker and go-live gate source of truth
- `live-saas-e2e-tracker-runbook.md` — End-to-end testing runbook (Wave 1 to Wave 9; Tests 01 to 35)
- `post-test-logical-solutions-backlog.md` — Control-family implementation backlog template for post-test logical-solution planning
- `root-key-safe-remediation-spec.md` — Planned root-key safe remediation state machine, API contract, data model, and acceptance tests
- `root-key-safe-remediation-acceptance-matrix.md` — MVP/Safe Rollout/GA gate matrix with pass/fail and evidence requirements
- `root-key-safe-remediation-implementation-checklist.md` — Serial implementation slices for the root-key safe remediation plan
- `test-results-workspace.md` — Evidence and artifact storage standard for each run
- `test-case-template.md` — Per-test template (preconditions, API/UI evidence, assertions, tracker mapping)
- `wave-summary-template.md` — Per-wave summary template (counts, severity, gate updates)

## Supporting Runtime Folder

Execution outputs are stored in:

- `../test-results/live-runs/<RUN_ID>/`

## Recent Targeted Runs

- [Recompute pipeline local fix validation on March 24, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260324T183815Z-recompute-pipeline-fix-validation/notes/final-summary.md) — PASS for the recompute-specific blocker: current-head local validation against the shared live tenant/account now proves `scripts/recompute_account_actions.py` completes successfully on tenant `9f7616d8-af04-43ca-99cd-713625357b70` / account `696505809372` instead of hanging indefinitely, and a queued local worker `compute_actions` message now advances far enough to emit its follow-on `integration_sync` and `attack_path_materialization` jobs. The retained timings show the remaining dominant cost is still `group_projection` (`39713ms`) rather than security-graph sync (`5891ms`), so the recompute path is slow-but-attributable instead of stuck. No deploy was performed in this validation slice.
- [Phase 3 P1.6 live Jira system-of-record proof on March 24, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260321T202330Z-phase3-p1-6-live/notes/final-summary.md) — PASS after extending the retained March 21 run with the March 24 closure slice: production now proves Jira outbound sync, conflicting inbound webhook preservation of canonical `Action.status`, explicit drift recording, and reconciliation back to the tenant-configured preferred Jira status `In Progress` on real issue `KAN-7`. The closure also fixed two live blockers found during the rerun: the `attack_path_materialization` queue poison on Lambda/Python 3.12 and reconciliation dedupe that previously blocked repeat sync to the same canonical provider state after drift.
- [CloudTrail.1 live generate/download/apply validation on March 23, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260323T162259Z/notes/final-summary.md) — PARTIAL: production now supports the approval-gated create-if-missing `CloudTrail.1` flow end to end through PR-bundle generation, ZIP download, and successful local Terraform apply against account `696505809372`, including creation of multi-region trail `security-autopilot-trail` and S3 bucket `ocypheris-live-ct-20260323162333-eu-north-1`. The retained run also exposed two remaining gaps: the generated Terraform bundle unnecessarily drags in `hashicorp/null` on the inactive non-create path, and the family still does not fully converge because `eu-north-1` findings flip to `RESOLVED` while `us-east-1` remains `NEW` after the apply and refresh cycle.
- [SSM.7 and CloudTrail.1 live E2E on March 23, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260323T190500Z-ssm7-cloudtrail1-live-e2e/notes/final-summary.md) — PASS after deploy: `SSM.7` passed end to end on the live stack for `us-east-1` action `e6b1eac2-041c-4fb3-9a47-2525a3afa908` with remediation run `cea5c507-fbc1-41a9-941b-131429704ff3`, successful local bundle apply, successful live ingest/compute/reconcile, and final live action status `resolved`. The same run also captured the CloudTrail rollout transition: production initially rejected `trail_bucket_name` / `create_bucket_if_missing` as unknown fields, then after deploying the current backend runtime it correctly required `bucket_creation_acknowledged` and successfully generated deterministic executable run `3a6200f9-919d-43a4-a53c-92c79380329a` for the approved create-if-missing path.
- [Grouped callback drift-guard + metadata-only replay repair validation on March 23, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260323T033827Z-cloudtrail-s35-local-e2e/notes/final-summary.md) — PASS for the narrowed March 23 callback/deploy hardening scope: the checked-in serverless deploy path now guards against runtime/DB-head drift before and after Lambda rollout, a fresh current-head local replay of the retained `S3.5` metadata-only callback repaired the stale `run_not_successful` member projection to `run_finished_metadata_only`, a second replay returned idempotent `200 group_run_report_replay` in `3.92s`, and `pg_stat_activity` was clean with no `idle in transaction` backend left behind after replay.
- [CloudTrail.1 and S3.5 local live-data rerun on March 23, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260323T033827Z-cloudtrail-s35-local-e2e/notes/final-summary.md) — MIXED: current-head local validation against the shared live tenant/account data confirms `CloudTrail.1` now fails closed truthfully as `400 invalid_strategy_inputs` and flat cards show no pending-confirmation banner, while the current live `S3.5` family produces a `12`-member review-required metadata-only bundle with no truthful executable fix to project. The rerun also captured a narrower remaining bug: the metadata-only `S3.5` grouped run still remained `started` after local callback execution/replay attempts.
- [Grouped PR-bundle local follow-up rerun on March 23, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260323T031500Z-grouped-pr-bundle-local-rerun-followup/notes/final-summary.md) — PARTIAL but narrower than the earlier rerun: current-head follow-up validation against the shared live tenant/account data confirmed that `s3_bucket_lifecycle_configuration` and `sg_restrict_public_ports` now both reach grouped `finished` on corrected reruns, while the only remaining blockers in this four-family set are stale active remediation rows on the shared DB for `s3_bucket_access_logging` and `us-east-1 ebs_default_encryption`.
- [Grouped PR-bundle local current-head rerun on March 23, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260323T020030Z-grouped-pr-bundle-local-rerun/notes/final-summary.md) — PARTIAL: local current-head code was exercised against the shared live tenant/account data without a redeploy. `cloudtrail_enabled` in both regions now fails closed as `invalid_strategy_inputs` instead of `500`, `eu-north-1 ebs_default_encryption` and `s3_bucket_encryption_kms` both reached grouped `finished`, but `s3_bucket_lifecycle_configuration` and `sg_restrict_public_ports` still left grouped runs in `started`, and full reruns for `s3_bucket_access_logging` and `us-east-1 ebs_default_encryption` were blocked by already-active remediation rows on the shared Neon DB.
- Follow-up grouped PR-bundle fix shipped locally on March 23, 2026 after the retained March 22 reruns:
  - grouped `cloudtrail_enabled` and customer-managed `ebs_default_encryption` bundle creation now fail closed as structured `400 invalid_strategy_inputs` instead of falling through to live `500` generation errors
  - generated grouped callback wrappers now use bounded `curl` retries plus explicit HTTP-success checks, and every callback-enabled bundle now includes `replay_group_run_reports.sh` for deterministic replay from `.bundle-callback-replay/`
  - the checked-in and generated Terraform runners now keep `TF_DATA_DIR` stable across `init`, `plan`, and `apply`, which closes the retained `Required plugins are not installed` failure mode seen in the March 22 `eu-north-1` grouped bundles
  - grouped callback wrappers now emit a terminal failed `finished` callback on process exit or signal, so interrupted bundle runs stop leaving the group permanently stuck in `started`
  - grouped Terraform runners now bound each action folder with `ACTION_TIMEOUT_SECS` for both `plan` and `apply`, so a hung Terraform subprocess fails closed and still allows the wrapper to publish a terminal callback
  - stale grouped `pr_only` runs that are still `pending` after `10` minutes no longer block a new grouped PR-bundle create request, which unblocks reruns after abandoned or never-started queue rows
- Grouped flat-view follow-up contract fix shipped locally on March 23, 2026:
  - grouped member state now distinguishes `run_successful_pending_confirmation` from `run_not_successful`, so finished grouped executions can surface the standard `This fix was applied successfully...` waiting-for-AWS banner without pretending they are already confirmed
  - internal grouped callback reevaluation now preserves successful-but-unconfirmed member state instead of collapsing it back into the failure bucket
  - `/api/actions` control filtering and control-token search are now canonical/alias-aware, so flat queries for `S3.13` resolve the `S3.11` lifecycle family and `EC2.19` resolves the canonical `EC2.53` security-group family
- Grouped metadata-only truthfulness fix shipped locally on March 23, 2026:
  - grouped member state now distinguishes terminal metadata-only/review-required completion as `run_finished_metadata_only` instead of collapsing it into `run_not_successful` or `not_run_yet`
  - flat findings and grouped findings now carry authoritative grouped status fields, so `latest_pr_bundle_run_id` remains navigation-only and no longer implies execution failure or “not run”
  - pending-confirmation banners are now reserved for executable finished runs that are awaiting AWS confirmation; metadata-only families like current live `S3.5` stay terminal review-only with no waiting-for-AWS banner
  - scoped repair path is now available via `PYTHONPATH=. ./venv/bin/python scripts/reproject_action_group_state.py --tenant-id <TENANT_UUID> --account-id 696505809372 --action-type s3_bucket_require_ssl --action-type cloudtrail_enabled --action-type ebs_default_encryption`
- Grouped findings scope split shipped locally on March 24, 2026:
  - `GET /api/findings/grouped` now returns one row per `(control_id, resource_type, account_id, region)` scope instead of collapsing mixed-region findings into one card
  - grouped findings `group_key` is now a scope-specific opaque identifier and the frontend no longer parses it for control labels or expansion filters
  - expanding a grouped findings card now reloads only that card's scoped findings, including `resource_type`, which keeps pending-confirmation notes like `EC2.182` aligned with the exact region/account/resource family that generated the bundle
  - live production verification after deploy (`backend image 20260324T042908Z`, `frontend version b8d6b016-78e2-4aff-bae6-60bd8eab9d3f`) confirmed `EC2.182` renders separate `eu-north-1` and `us-east-1` cards and the expanded `eu-north-1` snapshot-block-public-access card now requests `/api/findings?...&resource_type=AwsEc2SnapshotBlockPublicAccess...` and returns only the matching finding
- Follow-up fix shipped on March 22, 2026 local workspace after the retained all-groups run:
  - grouped PR-bundle member/finding surfaces now show a pending-confirmation note when execution finished but AWS source-of-truth confirmation is still outstanding, escalating after `12` hours
  - grouped bundle runners now execute each Terraform action folder inside its own isolated temporary workspace so local `.terraform` and state files do not collide across folders
- [All available grouped PR bundles live rerun on March 22, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260322T232408Z-all-groups-pr-bundle-live/notes/final-summary.md) — PARTIAL: the live account `696505809372` still exposed `22` grouped action families; `11` reached grouped status `finished`, `6` generated and executed locally but did not finish cleanly (`ebs_default_encryption`, `s3_bucket_access_logging`, `s3_bucket_encryption_kms`, `s3_bucket_lifecycle_configuration`, `s3_bucket_require_ssl`, and `sg_restrict_public_ports` in `eu-north-1`), and `5` failed earlier at bundle creation (`cloudtrail_enabled` in both regions, `ebs_default_encryption` in `us-east-1`, and `iam_root_access_key_absent` in both regions). This rerun confirmed that `s3_bucket_access_logging` now creates successfully when `log_bucket_name` safe defaults are supplied, but the grouped run still remained `started`.
- [All available grouped PR bundles live run on March 22, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260322T191802Z-all-groups-pr-bundle-live/notes/final-summary.md) — PARTIAL: the live account `696505809372` exposed `22` grouped action families; `15` completed end to end with grouped status `finished`, `1` (`sg_restrict_public_ports` in `eu-north-1`) generated successfully but recorded grouped execution `failed`, and `6` failed earlier at bundle creation due live `400/500` contract gaps (`cloudtrail_enabled`, `ebs_default_encryption` in `us-east-1`, `iam_root_access_key_absent`, and `s3_bucket_access_logging`). The run also confirmed that grouped callback persistence can succeed in the database even when the generated `run_all.sh` `curl` hangs client-side.
- [Phase 3 P1.6 live rerun on March 21, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260321T202330Z-phase3-p1-6-live/notes/final-summary.md) — FAIL for strict end-to-end provider proof: the live remediation system-of-record bug was fixed, validated locally, and redeployed to image tag `20260321T203011Z`, but the remaining Jira live proof is blocked because the supplied Atlassian API token returns `401 AUTHENTICATED_FAILED`, so the live tenant still cannot be configured safely for outbound/inbound provider sync.
- [Attack-path account-scope live rerun on March 21, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260321T185416Z-attack-path-account-scope-live-rerun/notes/final-summary.md) — PASS after backend redeploy to image tag `20260321T185016Z`: representative live `SSM.7`, `IAM.4`, and `Config.1` actions now return `attack_path_view.status="partial"` instead of `context_incomplete`, and the authenticated live `IAM.4` action-detail page now renders the bounded partial story instead of the old fail-closed relationship-context message. No scoped recompute was required.
- [Attack-path account-scope live E2E on March 21, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260321T184710Z-attack-path-account-scope-live-e2e/notes/final-summary.md) — FAIL on live production: the authenticated browser still renders the fail-closed `Relationship context is incomplete...` message for live `IAM.4`, and the matching live action-detail API confirms `graph_context.status="available"` while `attack_path_view.status="context_incomplete"` because `score_components.relationship_context` is still `null` on the live action rows for representative `SSM.7`, `IAM.4`, and `Config.1` actions.
- [Notification center live E2E rerun and production recovery on March 20-21, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260320T232930Z/notes/final-summary.md) — PASS after redeploying the backend runtime and applying the missing `0044_help_desk_platform` migration: the live Accounts-page Security Hub refresh now queues into the unified bell, the job progresses through `queued -> running -> success`, `Mark all read` clears the unread badge, archive removes the completed item, and `/api/notifications` plus `/api/notifications/jobs/{client_key}` both return `200` live.
- [Notification center live E2E validation on March 20, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260320T172617Z/notes/final-summary.md) — FAIL on live production rollout state: a real Accounts-page Security Hub refresh still queued and appeared in the old client-only bell, but `GET /api/notifications` returned `404` before and after the job trigger, the live browser never requested `/api/notifications`, and the deployed navbar bell is still the pre-unified `Notification Center` menu with `Clear finished` instead of the new responsive unified panel.
- [Accounts React `#418`, `/ready` queue-lag noise, and API cold-start cleanup on March 20, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260320T141626Z-accounts-ready-coldstart-fixes/notes/final-summary.md) — PASS on the live serverless runtime and live frontend: the Accounts page now renders with `0` console errors/warnings, the account detail modal opens cleanly, browser network capture shows `GET /api/auth/me => 200` and `GET /api/aws/accounts => 200`, `/ready` returns queue-lag metrics without raw `AccessDenied` noise, and the post-deploy API Lambda log window shows `0` timeout/import errors after the lazy first-invoke bootstrap change.
- [Template v1.5.9 / v1.4.7 live rollout recovery on March 20, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260320T044428Z/notes/final-summary.md) — PASS on the live serverless runtime and live frontend after fixing the bad auth/template-helper deploy mismatch: `GET /api/auth/me` now advertises `read-role/v1.5.9` and `write-role/v1.4.7`, the real connected account `696505809372` is back to `validated`, the Accounts page shows the account as Healthy again, and both the shared ReadRole connect flow and account-detail WriteRole flow preserve `param_SaaSExecutionRoleArns`.
- [Issue 2 authoritative live closure rerun on March 20, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260320T003851Z-issue2-live-closure-rerun/notes/final-summary.md) — PASS on the live serverless runtime and live frontend: after updating the real customer ReadRole stack for account `696505809372`, switching Lambda-originated customer-role assumptions to a session-name-only fallback, and fixing the live AWS Config validation probe, `POST /api/aws/accounts/696505809372/validate` now returns `validated`, `GET /api/aws/accounts` shows the account as `validated`, the Accounts page shows the account as Healthy, and the shared Launch Stack flows still preserve `param_SaaSExecutionRoleArns` for custom ReadRole and WriteRole stack names.
- [Issue 2 trust-boundary rollout and launch-link preservation live validation on March 19-20, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260319T230621Z-issue2-live-rollout-e2e/notes/final-summary.md) — PARTIAL on live serverless runtime and live frontend: the issue-2 rollout is now published at `read-role/v1.5.6` and `write-role/v1.4.4`, the live runtime advertises `param_SaaSExecutionRoleArns` and sends STS audit context, the live frontend was patched and redeployed so custom ReadRole/WriteRole Launch Stack links now preserve that execution-role parameter, but the existing connected customer account `696505809372` still remains in `error` until its customer-side ReadRole stack is updated to the new trust policy.
- [Issue 1 runtime guard live smoke on March 19, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260319T151815Z-issue1-runtime-guard-smoke/notes/final-summary.md) — PASS on live serverless runtime after redeploy to image tag `20260319T151052Z`: `/api/auth/me` now returned `read_role_template_url=v1.5.5` and `write_role_template_url=v1.4.3`, the real connected account `696505809372` returned `400` for `cleanup_resources=true` with the new fail-closed message, the account still existed immediately afterward, and the authenticated Accounts page rendered normally in a live browser session.
- [Remediation-profile Wave 6 grouped callback finalization proof on March 18, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260318T030658Z-rem-profile-wave6-live-closure-rerun/tests/w6-live-11.md) — PASS on current `master` `e9a362b3`: the retained local API reran grouped `S3.2`, `S3.9`, and `CloudTrail.1` customer-run bundles, all three group runs finished via `bundle_callback`, mixed `S3.2` / `S3.9` bundles persisted metadata-only non-executable result rows, no wrapper log showed `command not found` or `JSONDecodeError`, and the retained runtime plus queues were cleaned again after proof capture.
- [Remediation-profile Wave 6 Config.1 rollback restoration live proof on March 18, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260318T030658Z-rem-profile-wave6-live-closure-rerun/tests/w6-live-10.md) — PASS on current `master` `e9a362b3`: after patching the Config bundle local-exec env contract and reusing the retained grouped runner fixes, the grouped Config.1 bundle kept one executable action, live apply preserved recorder `default` while redirecting the delivery channel to the centralized bucket, bundle-local rollback restored the original recorder, delivery channel, and centralized bucket policy, the callback-managed group run reached `finished`, and the retained runtime plus queue family were cleaned back up again.
- [Remediation-profile Wave 6 S3.15 grouped SSE-KMS live proof closure on March 18, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260318T030658Z-rem-profile-wave6-live-closure-rerun/tests/w6-live-08.md) — PASS on current `master` `e9a362b3`: after adding bundle-local encryption capture/restore helpers and aligning grouped Terraform execution to the working local `hashicorp/aws 5.100.0` mirror, the grouped S3.15 bundle kept `10` AWS-managed actions executable and `1` customer-managed action review-only, live apply moved the 10 executable buckets to AWS-managed SSE-KMS, bundle-local rollback restored exact pre-remediation AES256 state on all executable buckets, and the callback-managed group run reached `finished`.
- [Remediation-profile Wave 6 retained closure rerun package on March 18, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260318T030658Z-rem-profile-wave6-live-closure-rerun/notes/final-summary.md) — PASS on current `master` `e9a362b3`: this is the authoritative Wave 6 closure gate. The retained March 18 package now records all nine families with `Live executable proof = Yes`, `Live downgrade/manual proof = Yes`, `Status = PASS`, and `Wave 6 complete = YES`, while the retained AWS fixtures plus runtime package are restored to baseline.
- [Remediation-profile Wave 6 S3.5 live follow-up on March 18, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260318T030658Z-rem-profile-wave6-live-closure-rerun/tests/w6-live-05.md) — PASS on current `master` `e9a362b3`: after persisting S3 preservation evidence into grouped action resolutions, the grouped S3.5 bundle again generated `7` executable plus `5` review-only actions, live apply preserved all existing bucket-policy statements while appending `DenyInsecureTransport`, bundle-local rollback restored exact pre-state on all seven executable buckets, the preservation-gated account action stayed review-only, and the clean rerun reached `finished`.
- [Remediation-profile Wave 6 final strict live completion gate on March 15-16, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260315T235627Z-rem-profile-wave6-live-aws-completion-gate/notes/final-summary.md) — historical failed strict gate on current `master` before the March 18 closure fixes: it captured the blocking `EC2.53`, `IAM.4`, `Config.1`, `S3.5`, `S3.11`, `S3.15`, and grouped callback defects that were later closed in the retained March 18 package above.
- [Remediation-profile Wave 6 strict blocker closure on March 15, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260315T231815Z-rem-profile-wave6-strict-blocker-closure/notes/final-summary.md) — historical readiness milestone on current `master` after commit `75fd66b2`: built a fresh isolated runtime plus strict S3 fixtures, corrected live family materialization to current Security Hub semantics (`S3.13 -> S3.11`, `S3.17 -> S3.15`, with drifted live `S3.11`/`S3.15` findings excluded), and established the final two family proofs that were later folded into the retained March 18 closure gate.
- [Remediation-profile Wave 6 environment readiness on March 15, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260315T201821Z-rem-profile-wave6-environment-readiness/notes/final-summary.md) — readiness-only run on current `master`, not the final Wave 6 E2E gate: rebuilt an isolated local runtime, enabled the IAM.4 authoritative route in runtime only, repaired the target-account customer-run credential path, seeded dedicated proof-friendly AWS resources in account `696505809372`, and captured a concrete family readiness matrix showing `EC2.53`, `S3.9`, `CloudTrail.1`, and `Config.1` now have executable-ready cases while `S3.2` and `S3.5` remain downgrade-only and `S3.11` / `S3.15` remain blocked by fresh Security Hub control drift.
- [Remediation-profile Wave 6 live AWS validation on March 15, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260315T175132Z-rem-profile-wave6-live-aws-e2e/notes/final-summary.md) — stop for fixes on current `master` under the archived-SaaS product model: the isolated local runtime plus isolated AWS accounts proved archived SaaS boundaries, multiple downgrade/manual branches, grouped mixed-tier metadata-only bundle behavior, and canonical resolver persistence, but no migrated family satisfied the required live executable plus downgrade proof; IAM.4 authoritative execution was disabled in the isolated runtime, EC2.53/S3.11/S3.15 had no fresh live cases, Config.1 lacked operator-owned write credentials for manual apply/rollback, and S3.9 grouped create exposed a blocking `500`.
- [Remediation-profile Wave 6 blocker-closure rerun on March 15, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260315T213821Z-rem-profile-wave6-readiness-rerun/notes/final-summary.md) — targeted rerun on current `master` using the resumed isolated runtime: closed the remaining code/evidence blockers for `S3.2` and `S3.5`, confirmed that live `S3.13` findings materialize executable `S3.11` while `S3.11` still lacks a truthful downgrade/manual live case, and reconfirmed that `S3.15` still lacks truthful live family materialization, so Wave 6 remains blocked under the strict two-proof standard.
- [Remediation-profile Wave 5 post-archive narrowed rerun on March 15, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260315T133714Z-rem-profile-wave5-post-archive-rerun/notes/final-summary.md) — PASS on current `master` under the archived-SaaS product model: the rerun exercised a fresh isolated local runtime plus isolated AWS queue account plumbing, proved `RPW5-POST-ARCHIVE-03` now accepts the first real mixed `finished` callback and persists both executable plus non-executable grouped results, kept replay/auth boundaries deny-closed, preserved mixed-tier grouped bundle generation and customer-run bundle semantics, and reconfirmed archived SaaS execution routes remain `410`.
- [Remediation-profile Wave 5 post-archive live AWS validation on March 15, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260315T125927Z-rem-profile-wave5-post-archive-live-aws-e2e/notes/final-summary.md) — stop for fixes under the archived-SaaS product model: current `master` proved real mixed-tier executable grouped bundle generation, customer-run bundle shape, deny-closed replay/auth boundaries, and archived SaaS execution routes, but `RPW5-POST-ARCHIVE-03` still failed because `download_bundle` worker sync finalized the group run at bundle-generation time before the mixed `finished` callback could persist executable plus non-executable results.
- [Remediation-profile Wave 5 live AWS validation on March 15, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260315T001855Z-rem-profile-wave5-live-aws-e2e/notes/final-summary.md) — stop for fixes: local `master` was exercised against an isolated local runtime and the real AWS account `696505809372`, proving Wave 5 mixed-tier manifest semantics, zero-executable non-500 behavior, and additive `non_executable_results[]` callback persistence, but the run found no real mixed-tier executable grouped family in the live dataset, no connected `WriteRole` for plan/apply, and a replayed grouped callback token was accepted instead of rejected.
- [Remediation-profile Wave 4 focused local validation on March 14, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260314T193034Z-rem-profile-wave4-e2e/notes/final-summary.md) — ready for Wave 5 after the focused rerun on fixed `master`: rerun environment proof shows `HEAD=7d3cd53a` on a restarted local backend, `RPW4-06` no longer returns `500` (override-different now creates a distinct run and the repo-target variant degrades cleanly to explicit `grouped_active_run_conflict` once both anchors are occupied), and the adjacent queue-`v2` regressions `RPW4-04` and `RPW4-05` still pass with fresh direct queue and worker evidence; `RPW4-03` remains blocked only by the local dataset exposing one profile per strategy.
- [Remediation-profile Wave 2 focused local validation on March 14, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260314T144353Z-rem-profile-wave2-e2e/notes/final-summary.md) — partial local proof: all targeted Wave 2 API contracts passed on `codex/rem-profile-w2-integrate`, including options/preview profile metadata, create-time canonical `artifacts.resolution`, legacy run hydration, duplicate guard, direct-fix regression smoke, auth boundaries, and remediation-settings influence, but the local `/actions/[id]` UI route hit an `ActionDetailDrawer` hydration mismatch that blocked visual remediation-options/remediation-preview validation and kept the gate decision at `stop for fixes`.
- [Phase 3 P2 grouped-action + UI closure validation on March 12, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260312T165611Z-phase3-p2-grouped-fix-validation/notes/final-summary.md) — PASS: the remaining March 12 P2 blockers are now closed on production. The filtered grouped findings API maps the trusted synthetic Config finding to the correct action, standard recompute succeeds, no synthetic actions remain open after recompute, and the live drawer visibly renders `Threat-intel provenance`, `CVE-2026-9001`, `CISA KEV`, confidence, applied points, and decay.
- [Phase 3 P2 live rerun on March 12, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260312T152625Z-phase3-p2-live-rerun/notes/final-summary.md) — partial live proof: the rerun’s synthetic findings proved the core `P2.1` weighting and `P2.2` decay/provenance API contracts on production, and the fresh Playwright session showed all six synthetic actions plus a trusted-config action drawer at `Priority 26`, but the human-readable `exploit_signals` explanation is inaccurate on the trusted-config case, UI provenance is not surfaced, the standard recompute still fails on the production security-graph defect, and post-cleanup app state still retained `4` archived synthetic actions.
- [Phase 3 P2 live validation on March 12, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260312T143138Z-phase3-p2-live/notes/final-summary.md) — blocked at the threat-intel candidate gate: the live tenant currently has only `7` configuration-style Security Hub findings and `6` config remediation actions, none of the inspected action details exposes the P2 threat-intel or provenance fields, and AWS-side vulnerability/threat-intel scenarios are required before `P2.1` or `P2.2` can be positively validated.
- [Phase 3 P1 post-deploy validation on March 12, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260312T021444Z-phase3-p1-postdeploy/notes/final-summary.md) — PASS for the previously missing live P1 contract surface: both Lambdas now run image tag `20260312T020548Z`, the live DB now reports both `0042` heads, `graph_context` / `business_impact` / `recommendation` are live, repo-aware PR artifacts now generate successfully, and only full P1.6 drift reconciliation remains blocked by the tenant having no configured test provider.
- [Phase 3 P1 live validation on March 12, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260312T013435Z-phase3-p1-live/notes/final-summary.md) — `NO` for full live validation right now: only the externally observable P1.4 rejection behavior passed; P1.1/P1.2/P1.3/P1.5/P1.7/P1.8 failed on missing live contracts or artifacts, and P1.6 was blocked because the integration/sync surface is absent on the deployed runtime.
- [P0.8 PR-only live validation on March 11, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260311T231949Z-p0-8-pr-only-live/notes/final-summary.md) — PASS: created live PR-only remediation run `88e08e11-0b86-4f7d-bf4e-fd24a5870ad1` for action `0ca64b94-9dcb-4a97-91b0-27b0341865bc`, run completed `success`, action detail now exposes executable `implementation_artifacts[]`, and run detail exposes populated `artifact_metadata` plus closure/evidence metadata without using `WriteRole`.
- [P0.3 post-deploy live validation on March 11, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260311T224012Z-p0-3-postdeploy-validation/notes/final-summary.md) — PASS: backend and worker redeployed from tag `20260311T181012Z` to `20260311T224136Z`, scoped backfill/recompute updated `26` live findings, both target findings now show complete `relationship_context`, and anchor action `442e46ac-f31c-4242-82ca-9e47081a3adb` shows a live toxic-combination boost from `69` to `84`.
- [P0.3 relationship-context producer-path validation on March 11, 2026 UTC](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260311T223155Z-p0-3-relationship-context-validation/notes/final-summary.md) — blocked early: scoped dry-run proved `26` live findings are in scope for backfill, but both live Lambdas still run image tag `20260311T181012Z`, which predates the March 12 producer-path implementation, so no live backfill/recompute was executed.
- [Phase 3 P0 live validation on March 11, 2026](/Users/marcomaher/AWS%20Security%20Autopilot/docs/test-results/live-runs/20260311T215615Z-phase3-p0-live/notes/final-summary.md) — partial validation on live prod: `P0.1`, `P0.2`, `P0.4`, `P0.5`, `P0.6`, and `P0.7` passed; `P0.3` and `P0.8` were not testable from current live data; supplied browser password was rejected on live.

Generate a new run scaffold with:

```bash
bash scripts/init_live_e2e_run.sh
```

## Grouped Callback Replay Notes

- Metadata-only grouped callbacks are terminal when they carry only `non_executable_results[]`; they should finalize the group run as `finished` and project member state to `run_finished_metadata_only`.
- `POST /api/internal/group-runs/report` now treats already-terminal matching payloads as replay-safe idempotent success instead of conflict, and it will repair missing/stale member projection when the run/results are already terminal but `action_group_action_state` is still behind.
- Use the retained `.bundle-callback-replay/` payloads with a freshly signed token when validating against a local current-head API; historical retained tokens can fail locally if the signing secret has rotated.
- The scoped maintenance helper now supports stuck grouped-run repair:

```bash
PYTHONPATH=. ./venv/bin/python scripts/reproject_action_group_state.py \
  --tenant-id <TENANT_UUID> \
  --account-id 696505809372 \
  --action-type s3_bucket_require_ssl \
  --action-type cloudtrail_enabled \
  --action-type ebs_default_encryption \
  --repair-stuck-runs
```
