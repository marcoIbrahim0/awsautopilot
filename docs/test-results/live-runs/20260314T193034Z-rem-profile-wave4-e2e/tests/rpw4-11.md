# Test RPW4-11 - action-detail UI still renders remediation options and preview

- Wave: `RPW4`
- Date (UTC): `2026-03-14T20:17:41Z`
- Tester: `Codex (GPT-5)`
- Frontend URL: `http://localhost:3000`
- Backend URL: `http://127.0.0.1:18002`

## Preconditions

- Identity: `Playwright CLI session with tenant-admin bearer injected on routed API requests`
- Tenant: `9f7616d8-af04-43ca-99cd-713625357b70`
- AWS Account: `696505809372`
- Region(s): `us-east-1`
- Required prior artifacts: `npx` available, `CODEX_HOME` and `PWCLI` configured, master frontend running on `:3000`, local tenant action `0ca64b94-9dcb-4a97-91b0-27b0341865bc`

## Steps Executed

1. Verified `npx` availability and launched Playwright CLI through `"$PWCLI"`.
2. Opened the current master frontend on `http://localhost:3000`.
3. Routed local frontend API calls from `http://localhost:8000/*` to the isolated backend on `http://127.0.0.1:18002/*` and injected the validated tenant-admin bearer plus CSRF header into routed API requests.
4. Set `localStorage.dev_tenant_id=9f7616d8-af04-43ca-99cd-713625357b70`.
5. Navigated to `/actions/0ca64b94-9dcb-4a97-91b0-27b0341865bc`.
6. Captured the healthy action-detail screenshot and console/network artifacts.
7. Clicked `Generate PR bundle` and captured the remediation preview screenshot and final clean console log.

## API Evidence

| # | Method | Endpoint | Request Payload | HTTP | Response Summary | Timestamp (UTC) | Artifact Path |
|---|---|---|---|---|---|---|---|
| 1 | `GET` | `/api/auth/me` | Routed Playwright request with injected tenant-admin bearer | `200` | Auth bootstrap succeeded in the final harnessed pass | `2026-03-14T20:15:04Z` | `evidence/ui/rpw4-11-network.log` |
| 2 | `GET` | `/api/actions/0ca64b94-9dcb-4a97-91b0-27b0341865bc` | Routed Playwright request with injected tenant-admin bearer | `200` | Action detail payload loaded successfully | `2026-03-14T20:15:04Z` | `evidence/ui/rpw4-11-network.log` |
| 3 | `GET` | `/api/aws/accounts` | Routed Playwright request with injected tenant-admin bearer | `200` | Account metadata loaded for the action detail route | `2026-03-14T20:15:04Z` | `evidence/ui/rpw4-11-network.log` |

## UI Evidence

| Screen/Flow | Expected | Observed | Screenshot Path |
|---|---|---|---|
| `/actions/[id]` healthy render | Action detail should render real content, not a blank/skeleton drawer | Full modal rendered with remediation controls, attack-path/storyboard sections, and remediation history | `evidence/screenshots/rpw4-11-action-detail-healthy.png` |
| `Generate PR bundle` preview | Remediation preview/workflow should open and stay usable | Preview modal opened with strategy chooser, dependency checks, execution timing, and CTA | `evidence/screenshots/rpw4-11-pr-bundle-preview.png` |
| Browser console | No obvious hydration mismatch or runtime error | Final authenticated preview pass logged `0` errors | `evidence/ui/rpw4-11-preview-console.log` |

## Assertions

- Positive path: `pass` - the action-detail route rendered real remediation content on current master.
- Negative path: `pass` - the route did not stay blank, skeleton-only, or stuck in the earlier drawer failure mode.
- Auth boundary: `pass` - the UI check used authenticated same-tenant requests only; tenant isolation itself is covered separately in `RPW4-12`.
- Contract shape: `pass` - remediation options were visible and the PR-bundle preview modal remained usable.
- Idempotency/retry: `n/a`
- Auditability: `pass` - the final console capture showed no obvious hydration mismatch or runtime error.

## Result

- Status: `PASS`
- Severity (if issue found): `n/a`
- Primary tracker mapping: `Scoped remediation-profile Wave 4 matrix / RPW4-11`

## Tracker Updates Applied

- Quick Status Board updated: `no`
- Section 8 go-live checkbox updated: `no`
- Section 9 changelog update required: `no`

## Notes

- Queue payload evidence source: `n/a`
- Worker evidence source: `n/a`
- Evidence type: `direct UI render` using Playwright CLI.
- The local frontend harness required route rewriting plus auth-header injection because the current master frontend hardcodes `http://localhost:8000` for local API traffic and expects a cookie-backed browser session. This was sufficient to prove the current master UI render path without changing application code.
