# Test RPW4-09 - unsupported future schema version still fails closed

- Wave: `RPW4`
- Date (UTC): `2026-03-14T20:01:05Z`
- Tester: `Codex (GPT-5)`
- Frontend URL: `http://localhost:3000`
- Backend URL: `http://127.0.0.1:18002`

## Preconditions

- Identity: `local isolated worker/runtime only`
- Tenant: `9f7616d8-af04-43ca-99cd-713625357b70`
- AWS Account: `696505809372`
- Region(s): `eu-north-1`
- Required prior artifacts: isolated ingest and quarantine queues

## Steps Executed

1. Injected a synthetic remediation-run message with `schema_version=99` into the isolated ingest queue.
2. Started the isolated worker against the dedicated queues.
3. Verified the worker quarantined the unsupported message instead of processing it.

## API Evidence

| # | Method | Endpoint | Request Payload | HTTP | Response Summary | Timestamp (UTC) | Artifact Path |
|---|---|---|---|---|---|---|---|
| 1 | `SQS inject` | isolated ingest queue | Synthetic remediation-run job with `schema_version=99` | `n/a` | Message accepted into isolated queue for contract test only | `2026-03-14T20:01:00Z` | `evidence/queue/rpw4-09-injected-message.json` |
| 2 | `SQS quarantine` | isolated quarantine queue | Quarantined unsupported remediation-run message | `n/a` | Quarantine reason is `unsupported_schema_version` with supported versions `1,2` | `2026-03-14T20:01:37Z` | `evidence/queue/rpw4-09-quarantine-message.json` |

## UI Evidence

| Screen/Flow | Expected | Observed | Screenshot Path |
|---|---|---|---|
| Worker-only contract check | No UI required | Not exercised | `n/a` |

## Assertions

- Positive path: `pass` - unsupported future schema versions are rejected and quarantined.
- Negative path: `pass` - the worker did not process the invalid job as a valid remediation run.
- Auth boundary: `n/a` - this was a local queue-injection contract test.
- Contract shape: `pass` - quarantine payload records `reason_code=unsupported_schema_version` and `reason_detail=job_type=remediation_run schema_version=99 supported_versions=1,2`.
- Idempotency/retry: `n/a`
- Auditability: `pass` - quarantine evidence was persisted and worker logs recorded the contract violation.

## Result

- Status: `PASS`
- Severity (if issue found): `n/a`
- Primary tracker mapping: `Scoped remediation-profile Wave 4 matrix / RPW4-09`

## Tracker Updates Applied

- Quick Status Board updated: `no`
- Section 8 go-live checkbox updated: `no`
- Section 9 changelog update required: `no`

## Notes

- Queue payload evidence source: `evidence/queue/rpw4-09-quarantine-message.json` and `evidence/queue/rpw4-09-injected-message.json`.
- Worker evidence source: `evidence/worker/master-worker.log`.
- Evidence type: `direct queue` and `direct worker log`.
- This was executed only against the isolated local queues; no production or shared queue was mutated.
