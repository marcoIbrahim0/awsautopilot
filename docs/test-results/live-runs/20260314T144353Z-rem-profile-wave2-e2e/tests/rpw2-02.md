# Test RPW2-02 - remediation-preview strategy-only compatibility

- Wave: `Remediation-profile Wave 2`
- Date (UTC): `2026-03-14T15:03:35Z`
- Tester: `Codex (GPT-5)`
- Frontend URL: `http://localhost:3000`
- Backend URL: `http://127.0.0.1:8000`

## Preconditions

- Identity: `admin`
- Tenant: `9f7616d8-af04-43ca-99cd-713625357b70 / Valens`
- AWS Account: `696505809372`
- Region(s): `eu-north-1`
- Required prior artifacts: `action_id=2ea6f141-6134-4dcd-8c82-4f0d0b6e582d`, `strategy_id=cloudtrail_enable_guided`

## Steps Executed

1. Requested remediation preview with `mode=pr_only` and `strategy_id` only.
2. Verified the legacy preview shape and additive `resolution`.
3. Spot-checked the local UI route that should expose the same preview flow.

## API Evidence

| # | Method | Endpoint | Request Payload | HTTP | Response Summary | Timestamp (UTC) | Artifact Path |
|---|---|---|---|---|---|---|---|
| 1 | `GET` | `/api/actions/2ea6f141-6134-4dcd-8c82-4f0d0b6e582d/remediation-preview` | `mode=pr_only`, `strategy_id=cloudtrail_enable_guided` | `200` | Legacy preview fields remained; additive `resolution.profile_id` defaulted to `cloudtrail_enable_guided` | `2026-03-14T15:03:35Z` | `../evidence/api/rpw2-02-preview-strategy-only.json` |

## UI Evidence

| Screen/Flow | Expected | Observed | Screenshot Path |
|---|---|---|---|
| `/actions/2ea6f141-6134-4dcd-8c82-4f0d0b6e582d` preview flow | Action drawer should allow strategy preview from the remediation surface | Browser-authenticated API calls succeeded, but the local action drawer stayed in a hydration-broken skeleton state and did not expose the preview controls | `../evidence/screenshots/rpw2-action-detail-hydration.png` |

## Assertions

- Positive path: `PASS` - strategy-only preview returned `200`.
- Negative path: `PASS` - no fallback rewriting was needed; preview resolved the single compatibility profile directly.
- Auth boundary: `PASS` - preview was exercised only through authenticated admin access here; auth denials are covered separately in `RPW2-11`.
- Contract shape: `PASS` - `compliant`, `message`, `will_apply`, `impact_summary`, `before_state`, `after_state`, and `diff_lines` all remained present, and additive `resolution` was returned.
- Idempotency/retry: `PASS` - the preview remained deterministic for the same action/strategy pair.
- Auditability: `PARTIAL` - raw preview payload is captured, but visual preview confirmation was blocked by the local hydration error.

## Result

- Status: `PARTIAL`
- Severity (if issue found): `🟠 HIGH`
- Primary tracker mapping: `N/A - focused remediation-profile Wave 2 run outside legacy tracker`

## Tracker Updates Applied

- Quick Status Board updated: `no`
- Section 8 go-live checkbox updated: `no`
- Section 9 changelog update required: `no`

## Notes

- The API response exposed `resolution.profile_id=cloudtrail_enable_guided` and preserved the legacy preview contract.
- UI preview validation remains blocked by the same local `ActionDetailDrawer` hydration mismatch described in `../evidence/ui/rpw2-ui-observations.md`.
