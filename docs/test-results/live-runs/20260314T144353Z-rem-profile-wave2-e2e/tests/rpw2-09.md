# Test RPW2-09 - legacy run detail compatibility hydration

- Wave: `Remediation-profile Wave 2`
- Date (UTC): `2026-03-14T15:05:30Z`
- Tester: `Codex (GPT-5)`
- Frontend URL: `http://localhost:3000`
- Backend URL: `http://127.0.0.1:8000`

## Preconditions

- Identity: `admin`
- Tenant: `9f7616d8-af04-43ca-99cd-713625357b70 / Valens`
- AWS Account: `696505809372`
- Region(s): `eu-north-1`
- Required prior artifacts: `legacy_run_id=88e08e11-0b86-4f7d-bf4e-fd24a5870ad1`

## Steps Executed

1. Fetched a known pre-Wave-2 PR-only remediation run.
2. Verified canonical `artifacts.resolution` was absent.
3. Verified top-level `resolution` was synthesized conservatively from legacy mirrors.

## API Evidence

| # | Method | Endpoint | Request Payload | HTTP | Response Summary | Timestamp (UTC) | Artifact Path |
|---|---|---|---|---|---|---|---|
| 1 | `GET` | `/api/remediation-runs/88e08e11-0b86-4f7d-bf4e-fd24a5870ad1` | `auth=admin` | `200` | `artifacts` still lacked canonical `resolution`, but top-level `resolution` was synthesized from `selected_strategy=ebs_enable_default_encryption_aws_managed_kms_pr_bundle` without DB backfill | `2026-03-14T15:05:30Z` | `../evidence/api/rpw2-09-run-detail-legacy.json` |

## UI Evidence

| Screen/Flow | Expected | Observed | Screenshot Path |
|---|---|---|---|
| Legacy run hydration | API-only compatibility hydration validation | Not exercised via UI in this focused run | `N/A` |

## Assertions

- Positive path: `PASS` - legacy run detail returned `200`.
- Negative path: `PASS` - the backend did not fabricate canonical artifacts into the stored legacy record.
- Auth boundary: `PASS` - legacy run detail remained tenant-authenticated.
- Contract shape: `PASS` - synthesized `resolution` conservatively mirrored `selected_strategy` and used the legacy-hydration rationale.
- Idempotency/retry: `PASS` - repeated reads of the legacy run remain compatible without DB backfill.
- Auditability: `PASS` - legacy payload and synthesized compatibility view were captured.

## Result

- Status: `PASS`
- Severity (if issue found): `N/A`
- Primary tracker mapping: `N/A - focused remediation-profile Wave 2 run outside legacy tracker`

## Tracker Updates Applied

- Quick Status Board updated: `no`
- Section 8 go-live checkbox updated: `no`
- Section 9 changelog update required: `no`

## Notes

- This run already existed before the Wave 2 contract and served as a real legacy compatibility target.
