null_resource.aws_config_enablement: Refreshing state... [id=8303899379719612915]

Terraform used the selected providers to generate the following execution
plan. Resource actions are indicated with the following symbols:
-/+ destroy and then create replacement

Terraform will perform the following actions:

  # null_resource.aws_config_enablement is tainted, so must be replaced
-/+ resource "null_resource" "aws_config_enablement" {
      ~ id       = "8303899379719612915" -> (known after apply)
      ~ triggers = {
          ~ "create_local_bucket"       = "false" -> "true"
          ~ "delivery_bucket"           = "config-bucket-029037611564" -> "sa-live-d1-config-029037611564-0304013744"
            # (4 unchanged elements hidden)
        }
    }

Plan: 1 to add, 0 to change, 1 to destroy.

─────────────────────────────────────────────────────────────────────────────

Saved the plan to: tfplan

To perform exactly these actions, run the following command to apply:
    terraform apply "tfplan"
