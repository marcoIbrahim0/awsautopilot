# Web framework
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0,<1.0.0
python-multipart>=0.0.9,<1.0.0  # Required for UploadFile/Form endpoints
mangum>=0.17.0,<1.0.0  # ASGI adapter for AWS Lambda + API Gateway

# Database + migrations
sqlalchemy>=2.0.0,<3.0.0
alembic>=1.13.0,<2.0.0
asyncpg>=0.29.0,<1.0.0
psycopg2-binary>=2.9.9,<3.0.0
greenlet>=3.0.0,<4.0.0

# AWS
boto3>=1.34.0,<2.0.0

# Validation / config
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
email-validator>=2.0.0,<3.0.0  # Required for Pydantic EmailStr validation

# HTTP client (optional but useful)
httpx>=0.26.0,<1.0.0

pyjwt>=2.8.0,<3.0.0
bcrypt>=4.0.0,<5.0.0  # Password hashing (used directly; passlib dropped due to bcrypt 4.1+ compat)

# Testing
pytest>=7.0.0,<10.0.0
pytest-asyncio>=0.23.0,<1.0.0
